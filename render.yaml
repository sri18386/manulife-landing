# Render static site blueprint — deploy this folder as-is (no build step).
services:
  - name: manulife-landing
    type: web
    runtime: static
    buildCommand: ""
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
